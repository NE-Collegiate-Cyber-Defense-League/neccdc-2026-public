---
# - name: NTP - Update MaxPosPhaseCorrection
#   ansible.windows.win_regedit:
#     path: HKLM:\SYSTEM\CurrentControlSet\Services\W32Time\Config
#     name: MaxPosPhaseCorrection
#     data: 0xFFFFFFFF
#     type: dword
#     state: present

# - name: NTP - Set NTP Servers
#   ansible.windows.win_shell: 'w32tm /config /manualpeerlist:"{{ ntp_servers | default("2.pool.ntp.org 2.us.pool.ntp.org 1.pool.ntp.org") }}" /syncfromflags:manual /reliable:yes /update'

# - name: NTP - Stop w32time service
#   ansible.windows.win_service:
#     name: w32time
#     state: stopped

# - name: NTP - Start w32time service
#   ansible.windows.win_service:
#     name: w32time
#     state: started

# - name: NTP - Resync rediscover
#   win_shell: 'w32tm /resync /rediscover'

# - name: NTP - Resync force
#   win_shell: 'w32tm /resync /force'

- name: Set Server Time Zone
  ansible.windows.win_timezone:
    timezone: "{{ set_timezone | default('Eastern Standard Time') }}"