# Regionals Falco Client

https://falco.org/docs/setup/download/

Using the deb/rpm packages instead of from the package repository because falco does not have an IPv6 address and was not working with NAT64.
