# Kiosk

Build time: ~12 minutes

## Build
The packer builts two amis:
1. The first is fully configured with correct nginx files and Teleport/Falco/Alloy installed. This is what was initially deployed for teams
2. The second is what was used for the "New Kiosk" inject
