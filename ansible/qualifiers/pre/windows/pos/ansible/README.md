Note for these plays it is using powershell to import/modify the mysql database. If you want to use mysql ansible modules you will need to install the required python packages on the target host.

Requires Python packages:
- PyMySQL

Requires System packages:
mysql-client
mariadb-client